<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Covers /storage/emulated/0, the only volume this browser operates on.
         Deliberately no root-path entry: that would expose app-private and
         system directories through the provider. -->
    <external-path name="external_files" path="."/>
</paths>
